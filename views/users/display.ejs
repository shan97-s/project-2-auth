<% lessons.forEach(lesson => { if(edit!=3&&lesson.userId==user.id && lesson.id==curlsn.id){%>
   <% var n=2 %>
   <p><b><%=lesson.dataValues.name %></b></p>
   <p><%=lesson.dataValues.content %></p>
   <p><%=lesson.dataValues.desc %></p> 
   <form action="../2/<%=lesson.id %> " method="POST">
    <button type="submit">Delete</button>
   </form>
   <form action="../3/<%=lesson.id %> " method="POST">
      <button type="submit">Edit</button>
     </form>
   
        <% }else if (edit==3&&lesson.userId==user.id && lesson.id==curlsn.id){%> 
         <p><b><%=lesson.dataValues.name %></b></p><form action="/users/startup" method="POST"style="display:block"><label for="name">Lesson Name</label>
            <input type="name" name="name" id="name" value="<%=lesson.dataValues.name %>"><br/><label for="name" >content</label><textarea type="text" name="content" id="text"style="height:50px;" value="<%=lesson.dataValues.name %>"></textarea><br/><label for="name">created by</label>
            <input type="name" name="descrip" id="name"value="<%=lesson.dataValues.desc%>"></form><br><br>
          
         <form action="../4/<%=lesson.id %> " method="POST">
            <button type="submit">Update</button>
           </form>
         <%}else if(lesson.userId!=user.id && lesson.id==curlsn.id&& curlsn.usertype==user.usertype){%>
             <p><b><%=curlsn.dataValues.name %></b></p>
    <p><%=curlsn.dataValues.content %></p>
   <p><%=curlsn.dataValues.desc %> This is the one</p> 
        <%}else if(lesson.id==curlsn.id && user.usertype!=curlsn.usertype){%>
           <p><b><%=curlsn.dataValues.name %></b></p>
           <p><%=curlsn.dataValues.content %></p>
           <p><%=curlsn.dataValues.desc %></p>
           <form action="<% lesson.id %> " method="POST">
            <button type="submit">delete</button>
           </form>
           <form action="<% lesson.id %> " method="POST">
            <button type="submit">Edit</button>
           </form>
        <%}

        }) %>

    
   
<button></button>

